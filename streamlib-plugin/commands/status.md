---
description: Check broker health, version, and uptime
allowed-tools:
  - Bash
---

Run `./.streamlib/bin/streamlib broker status` and report the results.

If the broker is not running, suggest running `./scripts/dev-setup.sh`.

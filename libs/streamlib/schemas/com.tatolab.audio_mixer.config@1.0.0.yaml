# Copyright (c) 2025 Jonathan Fontanez
# SPDX-License-Identifier: BUSL-1.1
#
# JSON Type Definition (RFC 8927) schema for AudioMixer config

metadata:
  name: com.tatolab.audio_mixer.config
  version: 1.0.0
  description: "Configuration for mixing two mono signals into stereo"

properties:
  strategy:
    metadata:
      description: "Mixing strategy for combining signals"
    enum:
      - Sum
      - SumNormalized
      - SumClipped

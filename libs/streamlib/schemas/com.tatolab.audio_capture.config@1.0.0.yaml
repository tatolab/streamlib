# Copyright (c) 2025 Jonathan Fontanez
# SPDX-License-Identifier: BUSL-1.1
#
# JSON Type Definition (RFC 8927) schema for AudioCapture config

metadata:
  name: com.tatolab.audio_capture.config
  version: 1.0.0
  description: "Configuration for audio capture (macOS)"

optionalProperties:
  device_id:
    metadata:
      description: "Audio input device name. If None, uses default device"
    type: string

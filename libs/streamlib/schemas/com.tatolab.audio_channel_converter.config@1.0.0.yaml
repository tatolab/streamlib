# Copyright (c) 2025 Jonathan Fontanez
# SPDX-License-Identifier: BUSL-1.1
#
# JSON Type Definition (RFC 8927) schema for AudioChannelConverter config

metadata:
  name: com.tatolab.audio_channel_converter.config
  version: 1.0.0
  description: "Configuration for mono to stereo channel conversion"

properties:
  mode:
    metadata:
      description: "Channel conversion mode"
    enum:
      - Duplicate
      - LeftOnly
      - RightOnly

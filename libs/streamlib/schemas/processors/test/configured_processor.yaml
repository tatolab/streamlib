name: com.streamlib.test.configured
version: 1.0.0
description: "Test processor with config for attribute macro tests"
execution:
  type: continuous
  interval_ms: 0

config:
  name: config
  schema: MyConfig

outputs:
  - name: data
    schema: com.tatolab.videoframe@1.0.0
    description: Video output

[package]
name = "streamlib-apple"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
streamlib-core = { path = "../streamlib-core" }
thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
tokio = { version = "1.0", features = ["rt", "time", "macros", "signal"] }
pollster = "0.3"  # For blocking on async in constructors
dispatch2 = "0.3"  # For GCD dispatch queues - must match objc2 version

# WebGPU for Metal ↔ WebGPU bridging
wgpu.workspace = true
wgpu-hal.workspace = true
metal = "0.31"  # Metal crate for wgpu-hal interop - must match wgpu-hal 25's version

# Modern objc2 ecosystem - comprehensive Apple framework bindings
objc2.workspace = true
objc2-metal.workspace = true
objc2-app-kit.workspace = true
objc2-io-surface.workspace = true
objc2-core-video.workspace = true
objc2-foundation.workspace = true
objc2-av-foundation.workspace = true
objc2-quartz-core.workspace = true
objc2-core-text.workspace = true
objc2-core-graphics.workspace = true

[dev-dependencies]
tracing-subscriber = "0.3"

[target.'cfg(target_os = "macos")'.dependencies]
# macOS-specific dependencies (if needed)

[target.'cfg(target_os = "ios")'.dependencies]
# iOS-specific dependencies (if needed)

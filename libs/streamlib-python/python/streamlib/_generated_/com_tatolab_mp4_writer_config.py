# Copyright (c) 2025 Jonathan Fontanez
# SPDX-License-Identifier: BUSL-1.1
#
# Generated from JTD schema using jtd-codegen. DO NOT EDIT.

# Code generated by jtd-codegen for Python v0.3.1

import re
from dataclasses import dataclass
from datetime import datetime, timedelta, timezone
from typing import Any, Dict, Optional, Union, get_args, get_origin


@dataclass
class Mp4writerConfig:
    """
    Configuration for MP4 file writer processor
    """

    output_path: 'str'
    """
    Path to output MP4 file
    """

    audio_bitrate: 'Optional[int]'
    """
    Audio bitrate in bits per second. Default: 128000 (128 kbps)
    """

    audio_codec: 'Optional[str]'
    """
    Audio codec identifier. Default: 'aac'
    """

    sync_tolerance_ms: 'Optional[float]'
    """
    A/V sync tolerance in milliseconds. Default: 33.3ms
    """

    video_bitrate: 'Optional[int]'
    """
    Video bitrate in bits per second. Default: 5000000 (5 Mbps)
    """

    video_codec: 'Optional[str]'
    """
    Video codec identifier. Default: 'avc1' (H.264)
    """


    @classmethod
    def from_json_data(cls, data: Any) -> 'Mp4writerConfig':
        return cls(
            _from_json_data(str, data.get("output_path")),
            _from_json_data(Optional[int], data.get("audio_bitrate")),
            _from_json_data(Optional[str], data.get("audio_codec")),
            _from_json_data(Optional[float], data.get("sync_tolerance_ms")),
            _from_json_data(Optional[int], data.get("video_bitrate")),
            _from_json_data(Optional[str], data.get("video_codec")),
        )

    def to_json_data(self) -> Any:
        data: Dict[str, Any] = {}
        data["output_path"] = _to_json_data(self.output_path)
        if self.audio_bitrate is not None:
             data["audio_bitrate"] = _to_json_data(self.audio_bitrate)
        if self.audio_codec is not None:
             data["audio_codec"] = _to_json_data(self.audio_codec)
        if self.sync_tolerance_ms is not None:
             data["sync_tolerance_ms"] = _to_json_data(self.sync_tolerance_ms)
        if self.video_bitrate is not None:
             data["video_bitrate"] = _to_json_data(self.video_bitrate)
        if self.video_codec is not None:
             data["video_codec"] = _to_json_data(self.video_codec)
        return data

def _from_json_data(cls: Any, data: Any) -> Any:
    if data is None or cls in [bool, int, float, str, object] or cls is Any:
        return data
    if cls is datetime:
        return _parse_rfc3339(data)
    if get_origin(cls) is Union:
        return _from_json_data(get_args(cls)[0], data)
    if get_origin(cls) is list:
        return [_from_json_data(get_args(cls)[0], d) for d in data]
    if get_origin(cls) is dict:
        return { k: _from_json_data(get_args(cls)[1], v) for k, v in data.items() }
    return cls.from_json_data(data)

def _to_json_data(data: Any) -> Any:
    if data is None or type(data) in [bool, int, float, str, object]:
        return data
    if type(data) is datetime:
        return data.isoformat()
    if type(data) is list:
        return [_to_json_data(d) for d in data]
    if type(data) is dict:
        return { k: _to_json_data(v) for k, v in data.items() }
    return data.to_json_data()

def _parse_rfc3339(s: str) -> datetime:
    datetime_re = '^(\d{4})-(\d{2})-(\d{2})[tT](\d{2}):(\d{2}):(\d{2})(\.\d+)?([zZ]|((\+|-)(\d{2}):(\d{2})))$'
    match = re.match(datetime_re, s)
    if not match:
        raise ValueError('Invalid RFC3339 date/time', s)

    (year, month, day, hour, minute, second, frac_seconds, offset,
     *tz) = match.groups()

    frac_seconds_parsed = None
    if frac_seconds:
        frac_seconds_parsed = int(float(frac_seconds) * 1_000_000)
    else:
        frac_seconds_parsed = 0

    tzinfo = None
    if offset == 'Z':
        tzinfo = timezone.utc
    else:
        hours = int(tz[2])
        minutes = int(tz[3])
        sign = 1 if tz[1] == '+' else -1

        if minutes not in range(60):
            raise ValueError('minute offset must be in 0..59')

        tzinfo = timezone(timedelta(minutes=sign * (60 * hours + minutes)))

    second_parsed = int(second)
    if second_parsed == 60:
        second_parsed = 59

    return datetime(int(year), int(month), int(day), int(hour), int(minute),
                    second_parsed, frac_seconds_parsed, tzinfo)            

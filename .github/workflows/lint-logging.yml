name: Lint Logging

# Enforces the unified logging pathway — see docs/logging.md.
# `cargo xtask lint-logging` bans the banned-macro/function set across all three
# surface areas without compiling the workspace:
# - Rust: syn-based AST walk over opt-in `libs/*` crates, honors
#   `#[allow(clippy::disallowed_macros)]`, `#[cfg(test)]`, and platform cfg
#   gates exactly as clippy would on `ubuntu-latest`.
# - Python: banned `print()`/`sys.stdout`/`logging.basicConfig` in
#   libs/streamlib-python.
# - TypeScript: banned `console.*`/`Deno.stdout.write` in libs/streamlib-deno.
#
# Rationale for not using `cargo clippy --workspace`: it transitively compiles
# `libs/vulkan-video`, whose build.rs requires `glslc`. Keeping the lockout
# compile-free lets this workflow run in seconds on a clean runner.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  xtask-lint-logging:
    name: xtask lint-logging (Rust + Python + TypeScript)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-xtask-${{ hashFiles('xtask/Cargo.toml', 'xtask/Cargo.lock', 'Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-xtask-

      # Serverless registry: the workspace declares `vulkanalia = { registry = "tatolab" }`,
      # so xtask cannot build without the fork fetchable. Emit it + reshape into a cargo
      # local-registry `[source]` replacement (no HTTP server) so `--locked` resolves offline.
      - name: Mirror vulkanalia fork (serverless cargo [source] replacement)
        uses: ./.github/actions/cargo-fork-mirror

      - name: cargo xtask lint-logging
        run: cargo run --locked -q -p xtask -- lint-logging

      - name: cargo test -p xtask lint_logging (fixture tests)
        run: cargo test --locked -p xtask lint_logging

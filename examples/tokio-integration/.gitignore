/target/

# The runtime's own working tree — JSONL logs, written where the app runs.
/.streamlib/

# Resolved per machine rather than pinned: an example should build against the
# engine it ships beside, not the one it was written against.
Cargo.lock

{
  "name": "camera-display",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "examples/camera-display/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build",
        "cwd": "examples/camera-display"
      }
    },
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo run",
        "cwd": "examples/camera-display"
      }
    }
  },
  "tags": ["rust", "example", "camera"]
}

# Camera Display

Rust example showing camera capture and display using streamlib.

## Running

```bash
cd examples/camera-display
cargo run
```

## Requirements

- macOS (uses AVFoundation for camera)
- WebGPU-capable GPU

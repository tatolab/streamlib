{
  "name": "audio-test",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "examples/audio-test",
  "projectType": "application",
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run python main.py",
        "cwd": "examples/audio-test"
      }
    },
    "sync": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv sync",
        "cwd": "examples/audio-test"
      }
    }
  },
  "tags": ["python", "example", "audio"]
}

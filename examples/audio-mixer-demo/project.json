{
  "name": "audio-mixer-demo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "examples/audio-mixer-demo/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build",
        "cwd": "examples/audio-mixer-demo"
      }
    },
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo run",
        "cwd": "examples/audio-mixer-demo"
      }
    }
  },
  "tags": ["rust", "example", "audio", "mixer"]
}

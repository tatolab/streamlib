[project]
name = "event-bus-demo"
version = "0.1.0"
description = "Event bus Python API demonstration"
requires-python = ">=3.10"
dependencies = [
    "streamlib",
]

[tool.uv.sources]
streamlib = { path = "../../libs/streamlib" }

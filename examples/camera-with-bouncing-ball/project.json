{
  "name": "camera-with-bouncing-ball",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "examples/camera-with-bouncing-ball",
  "projectType": "application",
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run python main.py",
        "cwd": "examples/camera-with-bouncing-ball"
      }
    },
    "sync": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv sync",
        "cwd": "examples/camera-with-bouncing-ball"
      }
    }
  },
  "tags": ["python", "example", "camera"]
}

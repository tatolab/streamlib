# Camera with Bouncing Ball

Example demonstrating camera capture with a bouncing ball overlay.

## Running

```bash
cd examples/camera-with-bouncing-ball
uv sync
uv run python main.py
```

## Requirements

- macOS (uses AVFoundation for camera)
- WebGPU-capable GPU

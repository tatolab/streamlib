; The @tatolab Deno packages (the streamlib SDK) resolve via the linked
; checkout: `streamlib link --engine <checkout>` (run by ../setup.sh) points
; the import map at the in-repo SDK, so no registry configuration is needed
; here.

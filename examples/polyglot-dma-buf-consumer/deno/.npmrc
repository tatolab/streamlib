; The @tatolab Deno packages (the streamlib SDK) resolve from npm by version
; once published. For local development, `streamlib link --engine <checkout>`
; (run by ../setup.sh) points the import map at the in-repo SDK instead, so no
; registry configuration is needed here — @tatolab resolves from the default
; npm registry.

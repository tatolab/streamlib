[project]
name = "simple-camera-display"
version = "0.1.0"
description = "Simple camera display example"
requires-python = ">=3.10"
dependencies = [
    "wgpu>=0.25.0",
    "glfw>=2.7.0",
]

[project.optional-dependencies]
dev = [
    "streamlib",
]

[tool.uv.sources]
streamlib = { path = "../../libs/streamlib" }

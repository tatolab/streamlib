# Simple Camera Display

Minimal example showing camera capture and display.

## Running

```bash
cd examples/simple-camera-display
uv sync
uv run python main.py
```

## Requirements

- macOS (uses AVFoundation for camera)
- WebGPU-capable GPU

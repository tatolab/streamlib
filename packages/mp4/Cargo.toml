[package]
name = "streamlib-mp4"
version = "1.0.0"
edition = "2024"
authors = ["Jonathan Fontanez <fontanezj1@gmail.com>"]
description = "MP4 file writer processors for streamlib."
keywords = ["mp4", "video", "writer", "ffmpeg", "streamlib"]
categories = ["multimedia::video", "multimedia"]
repository = "https://github.com/tato123/streamlib"
license = "BUSL-1.1"

[lib]
# The crate root is generated from `processors/` before every cargo
# invocation (`cargo xtask generate-crate-roots` in-tree; the build
# orchestrator's pre-cargo staging step everywhere else). It is a build
# artifact — gitignored, and excluded from the packed `.slpkg`.
path = "_generated_rust_crate_root_/lib.rs"
name = "streamlib_mp4"
crate-type = ["rlib", "cdylib"]

[build-dependencies]
streamlib-jtd-codegen = {version = "0.9.6"}

[dependencies]
# Engine-free authoring SDK (never the `streamlib` facade) — runtime context
# views, processor traits, generated wire types. Depending on the plugin SDK
# by its real name keeps this cdylib from statically linking a second engine.
streamlib-plugin-sdk = {version = "0.9.6"}
streamlib-macros = {version = "0.9.6"}
streamlib-plugin-abi = {version = "0.9.6"}

serde = {version = "1.0", features = ["derive"]}
tracing = {version = "0.1.41", features = ["release_max_level_debug"]}

[workspace]

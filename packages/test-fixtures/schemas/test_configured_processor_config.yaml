# Copyright (c) 2025 Jonathan Fontanez
# SPDX-License-Identifier: BUSL-1.1
#
# Test-only config schema for the attribute-macro test fixture
# `TestConfiguredProcessor`. Verifies the `#[streamlib::sdk::processor]`
# macro emits the correct config-binding code against a real schema.

metadata:
  type: TestConfiguredProcessorConfig
  description: "Test config schema for the attribute macro tests."

properties:
  threshold:
    metadata:
      description: "Threshold value for the test processor."
    type: float32

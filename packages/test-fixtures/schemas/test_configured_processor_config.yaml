# Copyright (c) 2025 Jonathan Fontanez
# SPDX-License-Identifier: BUSL-1.1
#
# Test-only config schema for the `TestConfiguredProcessor` attribute-macro
# test fixture. Mirrors the declared shape in
# `tests/configured_processor_test.rs::ConfiguredProcessorConfig`.

metadata:
  type: TestConfiguredProcessorConfig
  description: "Test config schema for the attribute macro tests."

properties:
  threshold:
    metadata:
      description: "Threshold value for the test processor."
    type: float32
